:boot
echo Login in with username: rancher, password: rancher
echo To install: ros install -d /dev/sdz1
kernel http://${url}/${folder}/vmlinuz rancher.state.autoformat=[/dev/sdz] rancher.password=rancher
initrd http://${url}/${folder}/initrd
boot

